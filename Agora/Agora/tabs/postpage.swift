import SwiftUI

struct PostPage: View {
    @State var isPresented: Bool = false
    var body: some View {
        SexyBackgroundView().edgesIgnoringSafeArea(.all)
        
        
    }
}

#Preview {
    PostPage()
} 
